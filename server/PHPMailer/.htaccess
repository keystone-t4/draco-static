# PHPMailer's source files are only ever loaded server-side via require() from
# contact.php — nothing here needs to be reachable directly over HTTP.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
